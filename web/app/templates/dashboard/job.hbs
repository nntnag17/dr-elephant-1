{{!-- The user tabs. Tabs can be added and deleted --}}
{{user-tabs deleteTab="deleteTab" setTab="changeTab" addTab="addTab" usernames=model.usernames}}
{{#if loading}}
  {{loading-panel message="loading jobs..."}}
{{else}}
  {{job-list jobs=model.jobs}}
{{/if}}